# Lessons

This folder will contain lesson content authored as **YAML front-matter + Markdown** files.

The schema (CEFR level, prerequisites, vocabulary, drill types, etc.) and the loader will be defined in a later step. For now this is a placeholder.

When the format lands, see `src/batoiller/lessons/` for the parser and `docs/decisions/` for the schema rationale.
